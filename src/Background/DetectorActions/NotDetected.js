module.exports = function(tab) {
    // chrome.tabs.executeScript(tab.id,{
    //      code: `
    //         if (typeof MutationsManager !== "undefined") {
    //             window.MutationsManager.empty();
    //             delete window.MutationsManager;
    //         }
    //         if (typeof TasksManager !== "undefined") delete window.TasksManager;
    //         if (typeof PageController !== "undefined") delete window.PageController;`,
    //      runAt:"document_start"
    //  });
};
